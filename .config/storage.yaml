# =============================================================================
# AVM – Storage domain
#
# Resource modules: Storage Account
# Pattern modules:  Function App + private endpoint-secured Storage
# Utility modules:  (none in scope)
# =============================================================================

domain: storage
description: Storage accounts and storage-adjacent patterns

modules:

  # ---------------------------------------------------------------------------
  # Resource modules
  # ---------------------------------------------------------------------------

  - name: terraform-azurerm-avm-res-storage-storageaccount
    type: res
    url: https://github.com/Azure/terraform-azurerm-avm-res-storage-storageaccount.git
    branch: main
    description: Storage Account

  # ---------------------------------------------------------------------------
  # Pattern modules
  # ---------------------------------------------------------------------------

  - name: terraform-azurerm-avm-ptn-function-app-storage-private-endpoints
    type: ptn
    url: https://github.com/Azure/terraform-azurerm-avm-ptn-function-app-storage-private-endpoints.git
    branch: main
    description: Function App with private endpoint-secured Storage
