resource:
  type: azapi_resource_list
  provider: azapi
  symbol_type: datasource
  registry_url: "https://registry.terraform.io/providers/azure/azapi/latest/docs/data-sources/resource_list"

provider_updates:
  last_checked: "2026-05-13T06:21:02Z"
  findings:
  - version: "2.0.1"
    criticality: critical
    type: breaking_change
    summary: "`azapi_resource_list` data source's `output` field defaults to the response when the `response_export_values` is not specified."
    url: "https://github.com/Azure/terraform-provider-azapi/releases/tag/v2.0.1"
  - version: "1.13.1"
    criticality: medium
    type: enhancement
    summary: "`azapi_resource_list` data source: The `output` field changes from JSON string to HCL object. Users can use access the fields in the output as an HCL object. Please remove the `jsondecode` function..."
    url: "https://github.com/Azure/terraform-provider-azapi/releases/tag/v1.13.1"
  - version: "1.13.0"
    criticality: medium
    type: enhancement
    summary: "`azapi_resource` resource, `azapi_update_resource` resource, `azapi_resource_action` resource, `azapi_data_plane_resource` resource, `azapi_resource_action` data source, `azapi_resource` data sourc..."
    url: "https://github.com/Azure/terraform-provider-azapi/releases/tag/v1.13.0"

provider_issues:
  last_checked: null
  items: []
  # item shape:
  #   - number: 1234
  #     title: "..."
  #     labels: [bug]
  #     url: "..."
  #     created_at: "2026-01-01"

enrichment:
  notes: []
